// 1.- Utiliza 5 veces la función prompt; para solicitar los datos necesarios y almacena cada respuesta en una variable.
let // quita el comentario y define la variable...
let // quita el comentario y define la variable...
let // quita el comentario y define la variable...
let // quita el comentario y define la variable...
let // quita el comentario y define la variable...
alert("Muchas gracias por la informacion.");

// 2.- Inicializa 4 variables = null, una variable para cada una de las 4 condiciones a evaluar.
// ejemplo de variables: condicion_nombre, condicion_edad, condicion_altura, condicion_mascota
let // quita el comentario y define la variable... 
let // quita el comentario y define la variable...
let // quita el comentario y define la variable...
let // quita el comentario y define la variable...


// 3.- Utiliza if-else para evaluar si el espía tiene la misma primera letra en el nombre y el apellido.
// HINT: los string pueden ser tratados con arrays, por lo que puedes acceder a un caracter especifico de un string con su index.
/* Si el resultado de comparar la primera letra del nombre y del apellido como iguales es true: asigna true a la variable
   a la que le corresponde almacenar el resultado de esa comparación, siguiendo el ejemplo anterior: condicion_nombre = true. */
// Completa la sentencia if-else:
if () {
  
} else {
  
}

// 4.- Si la edad del espía está entre 20 y 30 años de edad, asigna true a tu variable equivalente a: condicion_edad
// Completa la sentencia if-else
if () {
  
} else {
  
}

// 5.- Si el espía mide 170 cm o más: asigna true a tu variable equivalente a: condicion_altura
// Completa la sentencia if-else
if () {
  
} else {
  
}

// 6.- Si la última letra del nombre de la mascota del espía termina con a: asigna  true a tu variable equivalente a: condicion_mascota
// HINT: para acceder al último elemento de un array o un string puede utilizar la siguiente sintaxis:
// nombre_del_array[nombre_del_array.length-1]
// nombre_del_array.length da la longitud del array o string y recuerda que empiezas a contar de 0, por eso le restamos 1. 
// Completa la sentencia if-else
if () {
  
} else {
  
}

// 7.- Si las 4 condiciones son verdaderas, imprime un mensaje oculto en la consola
// Completa la sentencia if
if () {
  // Mi mensaje secreto
  console.log("ingresa aquí el mensaje secreto")
} else {
}